from setuptools import setup, find_packages

setup(
    name = 'tube_searcher_bot',
    version = '0.1.0',
    description='crawl YouTube, and tweet video URL using Twitter API',
    author='to-lz1',
    packages = ['tube_searcher_bot'],
    test_suite = 'test_suite'
)
